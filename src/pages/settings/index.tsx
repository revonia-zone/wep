import PageTitle from "@/components/base/header/page-title";
import {Settings} from "lucide-react";

function SettingsPage() {
  return (
    <div>
      <PageTitle text="Settings" icon={Settings} />
    </div>
  )
}

export default SettingsPage;
